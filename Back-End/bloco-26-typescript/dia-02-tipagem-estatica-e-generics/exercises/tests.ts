type Bird = {
  bird: string,
}

type sumFunc = (a: number, b: number) => number;

type Address = {
  country: string,
  street: string,
  number: number,
};

type MaterState = 'liquid' | 'solid' | 'gasoso';

type cpf = number | string;

type OS = 'linux' | 'macOs' | 'windows';

type vogals = 'a' | 'e' | 'i' | 'o' | 'u';